[project]
name = "anvx-core"
version = "0.2.0"
description = "ANVX core engine - connectors, intelligence, analytics"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"anthropic>=0.86.0",
	"httpx>=0.28.1",
	"keyring>=25.0.0",
	"pydantic>=2.12.5",
	"python-dotenv>=1.2.2",
]

[tool.setuptools.packages.find]
where = ["src"]
