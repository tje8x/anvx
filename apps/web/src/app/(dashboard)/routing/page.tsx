import SectionTitle from '@/components/anvx/section-title'

export default function RoutingPage() {
  return (
    <div>
      <SectionTitle>Routing</SectionTitle>
      <p className="text-[11px] font-data text-anvx-text-dim">
        Mode cards, shadow recommendations, routing rules — coming in Week 3.
      </p>
    </div>
  )
}
